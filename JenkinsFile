pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo '📦 Pulling latest code from GitHub...'
                git branch: 'main', url: 'https://github.com/yourusername/mediguide.git'
            }
        }

        stage('Validate') {
            steps {
                echo '🔍 Checking HTML/JS syntax...'
                bat 'echo Validation successful (you can add eslint or htmlhint later)'
            }
        }

        stage('Deploy') {
            steps {
                echo '🚀 Deploying Mediguide...'
                bat 'echo Files copied to deployment directory (placeholder)'
                // You could do something like:
                // bat 'xcopy /Y /E * "C:\\inetpub\\wwwroot\\mediguide"'
            }
        }
    }

    post {
        success {
            echo '✅ Mediguide deployed successfully!'
        }
        failure {
            echo '❌ Build failed. Check logs for details.'
        }
    }
}

        }
    }
}

